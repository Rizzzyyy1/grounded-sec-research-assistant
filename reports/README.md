# reports/

`RESULTS.md`, `ablation_*.md` and `retrieval_*.md` are generated by `finsight eval ...` and
`scripts/collect_results.py` and are committed as evidence. `runs/` (per-question results) is
git-ignored: regenerate with `finsight eval run --system <rag|router|agent> --split <dev|test>`.
